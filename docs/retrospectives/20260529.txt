RETROSPECTIVA — 2026-05-29
Projeto: automations (orquestrador genérico publicável no GitHub)

=== O QUE FOI FEITO ===

Estrutura do projeto
  - Corrigida a estrutura de pastas: pyproject.toml na raiz, pacote Python em automations/automations/
  - pip install -e . funcionando corretamente
  - Entry point `automate` disponível globalmente

Fase 0 — validate (concluída)
  - config.py: Settings (pydantic-settings), ScheduleConfig, JobConfig, ResultConfig, Config, load_config
  - cli.py: comando `automate validate` funcionando
  - Validação de schema do jobs.yaml, fallback de python_exe, detecção de nomes duplicados

Fase 1 — run (concluída)
  - execution_log.py: persistência de estado em JSON (last_success, history, rotação de 30 entradas)
  - runner.py: elegibilidade (days_of_week, day_of_month, min_interval_hours), execução via subprocess, success/failure markers, timeout, registro de resultado
  - cli.py: comando `automate run` com flags --dry-run, --force, --job <nome>
  - examples/hello_job.py: script de exemplo para testes

Testes realizados
  - automate validate ✓
  - automate run --dry-run ✓
  - automate run --job hello ✓ (1.0s, SUCCESS)
  - automate run ✓ (hello SUCCESS, market_data FAILED — script inexistente, comportamento esperado)
  - execution_log.json persistindo corretamente ✓

=== O QUE ESTÁ PRONTO ===

  - Estrutura publicável no GitHub
  - Ciclo completo: jobs.yaml → elegibilidade → subprocess → execution_log.json
  - Qualquer script Python pode ser cadastrado sem escrever código — só YAML

=== O QUE FALTA ===

  Fase 2 — email consolidado após cada rodada (Settings já tem os campos SMTP)
  Fase 3 — automate schedule (loop interno, substitui Task Scheduler / cron)
  Produção — jobs.yaml com os scripts reais do madcap_env
  Qualidade — testes automatizados, README atualizado

=== DECISÕES TOMADAS ===

  - logs/ hardcoded (relativo ao cwd do automate) — sem configuração necessária
  - email_on_success: bool no Settings — controla se envia email em sucesso ou só em falha
  - Sucesso determinado por exit code 0; success_marker é edge case opcional
  - Paths de scripts ficam no jobs.yaml, não no .env
  - docs/retrospectives/ no .gitignore — arquivos pessoais, não vão pro repo